{% extends 'base.html.twig' %}

{% block title %}Capitale index{% endblock %}

{% block body %}
    <h1>Capitale index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for capitale in capitales %}
            <tr>
                <td>{{ capitale.id }}</td>
                <td>{{ capitale.nom }}</td>
                <td>
                    <a href="{{ path('capitale_show', {'id': capitale.id}) }}">show</a>
                    <a href="{{ path('capitale_edit', {'id': capitale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('capitale_new') }}">Create new</a>
{% endblock %}
