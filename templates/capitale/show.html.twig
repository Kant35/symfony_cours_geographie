{% extends 'base.html.twig' %}

{% block title %}Capitale{% endblock %}

{% block body %}
    <h1>Capitale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ capitale.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ capitale.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('capitale_index') }}">back to list</a>

    <a href="{{ path('capitale_edit', {'id': capitale.id}) }}">edit</a>

    {{ include('capitale/_delete_form.html.twig') }}
{% endblock %}
