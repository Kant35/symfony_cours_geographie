<form method="post" action="{{ path('capitale_delete', {'id': capitale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ capitale.id) }}">
    <button class="btn">Delete</button>
</form>
